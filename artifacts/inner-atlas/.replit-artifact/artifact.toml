kind = "web"
previewPath = "/"
title = "Inner Atlas"
version = "1.0.0"
id = "artifacts/inner-atlas"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24931

[services.development]
run = "pnpm --filter @workspace/inner-atlas run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/inner-atlas", "run", "build" ]
publicDir = "artifacts/inner-atlas/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24931"
BASE_PATH = "/"
